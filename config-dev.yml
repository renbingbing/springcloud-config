spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:renbingbing/springcloud-config.git
          search-paths: 
            - springcloud-config
      label: master
